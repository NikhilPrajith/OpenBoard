/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['liveblocks.io','images.unsplash.com',"pexels.com",'images.pexels.com'],
      },

};

export default nextConfig;
