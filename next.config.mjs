/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['liveblocks.io','images.unsplash.com',"pexels.com",'images.pexels.com','firebasestorage.googleapis.com'],
      },

};

export default nextConfig;
